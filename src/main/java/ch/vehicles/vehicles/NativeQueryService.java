package ch.vehicles.vehicles;

import org.springframework.stereotype.Service;

import javax.persistence.EntityManager;
import java.util.List;

@Service
public class NativeQueryService {
/*
    EntityManager em;

    public NativeQueryService(EntityManager em) {
        this.em = em;
    }

    public List<Vehicleone> findVehiclesByName(String name) {
        var query = em.createNativeQuery("SELECT * FROM VEHICLEONE WHERE NAME=?");
        query.setParameter(1, name);
    }
*/
}

